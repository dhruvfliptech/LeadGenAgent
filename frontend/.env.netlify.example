# Netlify Environment Variables Template for CraigLeads Pro Frontend
# Copy these to Netlify Dashboard: Site settings > Environment variables
#
# IMPORTANT: In Netlify, set these WITHOUT the VITE_ prefix in the dashboard,
# then reference them in your build with VITE_ prefix in netlify.toml if needed,
# OR set them directly with VITE_ prefix as shown below.

# ==============================================================================
# REQUIRED: Backend API Configuration
# ==============================================================================

# Backend API URL - MUST be set to your deployed backend URL
# Examples:
#   Railway: https://your-app-name.railway.app
#   Render: https://your-app-name.onrender.com
#   Heroku: https://your-app-name.herokuapp.com
VITE_API_URL=https://your-backend-url.railway.app

# WebSocket URL for real-time updates (usually same as API but with wss://)
# Examples:
#   Railway: wss://your-app-name.railway.app
#   Render: wss://your-app-name.onrender.com
VITE_WS_URL=wss://your-backend-url.railway.app

# ==============================================================================
# OPTIONAL: Application Configuration
# ==============================================================================

# Environment mode (production, staging, development)
VITE_ENVIRONMENT=production

# Application name (for branding)
VITE_APP_NAME=CraigLeads Pro

# Application version (syncs with package.json)
VITE_APP_VERSION=1.0.0

# ==============================================================================
# OPTIONAL: Feature Flags
# ==============================================================================

# Enable/disable features based on deployment environment
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_ERROR_REPORTING=true
VITE_ENABLE_DEBUG_MODE=false

# Enable specific modules
VITE_ENABLE_LINKEDIN=true
VITE_ENABLE_GOOGLE_MAPS=true
VITE_ENABLE_JOB_BOARDS=true
VITE_ENABLE_AI_FEATURES=true
VITE_ENABLE_VIDEO_FEATURES=true
VITE_ENABLE_CAMPAIGNS=true

# ==============================================================================
# OPTIONAL: External Services (if frontend needs direct access)
# ==============================================================================

# Google Analytics (if using)
# VITE_GA_TRACKING_ID=UA-XXXXXXXXX-X

# Sentry for error tracking (if using)
# VITE_SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# PostHog for product analytics (if using)
# VITE_POSTHOG_KEY=phc_xxxxxxxxxxxxxxxxxxxxx
# VITE_POSTHOG_HOST=https://app.posthog.com

# ==============================================================================
# OPTIONAL: API Configuration Overrides
# ==============================================================================

# API request timeout (milliseconds)
VITE_API_TIMEOUT=30000

# Enable API request logging
VITE_API_DEBUG=false

# API rate limit per minute
VITE_API_RATE_LIMIT=100

# ==============================================================================
# OPTIONAL: UI/UX Configuration
# ==============================================================================

# Default pagination limit
VITE_DEFAULT_PAGE_SIZE=50

# Maximum file upload size (bytes)
VITE_MAX_FILE_SIZE=10485760

# Toast notification duration (milliseconds)
VITE_TOAST_DURATION=5000

# ==============================================================================
# BUILD-TIME CONFIGURATION (Netlify Build)
# ==============================================================================

# Node version (set in netlify.toml, but can override here)
# NODE_VERSION=18

# Enable source maps in production (not recommended for security)
# VITE_GENERATE_SOURCEMAP=false

# ==============================================================================
# NOTES FOR NETLIFY DEPLOYMENT
# ==============================================================================
#
# 1. Go to Netlify Dashboard
# 2. Select your site
# 3. Navigate to: Site settings > Environment variables
# 4. Click "Add a variable" or "Edit variables"
# 5. Add each variable above (remove comments)
# 6. Variable scopes:
#    - All: Used for all contexts (production, deploy previews, branch deploys)
#    - Production: Only for production builds
#    - Deploy previews: Only for PR previews
#    - Branch deploys: Only for specific branches
#
# 7. MINIMUM REQUIRED for deployment:
#    - VITE_API_URL
#    - VITE_WS_URL
#
# 8. After adding variables, trigger a new deploy or Netlify will use them
#    on the next automatic deployment.
#
# 9. Environment variables are injected at BUILD TIME, not runtime.
#    Changes require a rebuild/redeploy.
#
# ==============================================================================
# VALIDATION CHECKLIST
# ==============================================================================
#
# Before deploying, ensure:
# [ ] VITE_API_URL points to your live backend
# [ ] VITE_WS_URL matches your backend WebSocket endpoint
# [ ] Backend CORS settings allow your Netlify domain
# [ ] Backend is deployed and accessible
# [ ] SSL certificates are active (https/wss)
# [ ] All feature flags match your backend capabilities
#
# ==============================================================================
