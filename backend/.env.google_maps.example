# Google Maps Business Scraper Configuration
# Copy this to your .env file and update the values

# ============================================================================
# Google Maps Scraper Settings (Playwright Mode)
# ============================================================================

# Enable/disable Google Maps scraping feature
GOOGLE_MAPS_ENABLED=true

# Maximum number of results to scrape per request (1-100)
# Recommendation: Keep at 20 for Playwright mode to avoid rate limits
GOOGLE_MAPS_MAX_RESULTS=20

# Scraping timeout in seconds (default: 300 = 5 minutes)
GOOGLE_MAPS_SCRAPE_TIMEOUT=300

# Delay between requests in seconds
# Recommendation: Keep at 2-5 seconds to appear more human-like
GOOGLE_MAPS_MIN_DELAY=2.0
GOOGLE_MAPS_MAX_DELAY=5.0

# Enable email extraction from business websites
# Warning: Adds ~5 seconds per business
GOOGLE_MAPS_ENABLE_EMAIL_EXTRACTION=true

# ============================================================================
# Google Places API Settings (Recommended for Production)
# ============================================================================

# Get your API key from: https://console.cloud.google.com/
# Enable "Places API" in your Google Cloud project
# Requires billing enabled (but you get $200 free credit/month)
GOOGLE_PLACES_API_KEY=

# ============================================================================
# Optional: Proxy Settings (for avoiding rate limits)
# ============================================================================

# Proxy server URL (format: http://user:pass@host:port)
# GOOGLE_MAPS_PROXY=

# ============================================================================
# Email Finder Integration (Phase 2 - Optional)
# ============================================================================

# Hunter.io - Email Finder Service
# Free tier: 100 searches/month
# Paid: $49/mo (1,000), $99/mo (5,000), $199/mo (20,000)
HUNTER_IO_ENABLED=false
HUNTER_IO_API_KEY=
HUNTER_MONTHLY_QUOTA=100
HUNTER_ALERT_THRESHOLD=80

# RocketReach - Alternative Email Finder (Better for B2B)
# Pricing: $50/mo (150 searches), $200/mo (1,000)
ROCKETREACH_ENABLED=false
ROCKETREACH_API_KEY=
ROCKETREACH_MONTHLY_QUOTA=150

# Email Finder Fallback Settings
EMAIL_FINDER_FALLBACK_TO_SCRAPING=true
EMAIL_FINDER_CACHE_ENABLED=true
EMAIL_FINDER_MIN_CONFIDENCE_SCORE=30

# ============================================================================
# Usage Examples
# ============================================================================

# Example 1: Free Mode (Playwright + Website Email Extraction)
# GOOGLE_MAPS_ENABLED=true
# GOOGLE_MAPS_MAX_RESULTS=20
# GOOGLE_MAPS_ENABLE_EMAIL_EXTRACTION=true
# Cost: $0/month
# Limitations: Slower, may face rate limits, 30-50% email success rate

# Example 2: Production Mode (Places API + Hunter.io)
# GOOGLE_MAPS_ENABLED=true
# GOOGLE_PLACES_API_KEY=your_api_key_here
# HUNTER_IO_ENABLED=true
# HUNTER_IO_API_KEY=your_hunter_api_key
# Cost: ~$49-99/month (depending on volume)
# Benefits: Fast, reliable, 70-90% email success rate

# Example 3: Budget Mode (Places API only, manual email lookup)
# GOOGLE_MAPS_ENABLED=true
# GOOGLE_PLACES_API_KEY=your_api_key_here
# GOOGLE_MAPS_ENABLE_EMAIL_EXTRACTION=false
# Cost: Free tier ($200 credit = 4,000 businesses/month)
# Benefits: Fast, reliable, good data quality

# ============================================================================
# Cost Estimates
# ============================================================================

# Playwright Mode (Free):
# - Cost per lead: $0
# - Speed: 3-8 seconds per business
# - Email success: 30-50% (if enabled)
# - Risk: May face rate limits/blocks

# Google Places API Mode:
# - Text Search: $32 per 1,000 requests
# - Place Details: $17 per 1,000 requests
# - Cost per lead: ~$0.049
# - Free tier: $200 credit = ~4,000 businesses/month
# - Speed: <1 second per business
# - Email success: N/A (need separate email finder)

# Hunter.io Email Finder:
# - Free: 100 searches/month
# - Starter ($49/mo): 1,000 searches/month
# - Growth ($99/mo): 5,000 searches/month
# - Pro ($199/mo): 20,000 searches/month
# - Success rate: 70-90%

# Total Monthly Costs:
# - Free tier: $0 (Playwright + 100 Hunter.io searches)
# - Light usage: $49/mo (Places API free + Hunter.io Starter)
# - Medium usage: $99/mo (Places API ~$50 + Hunter.io Growth)
# - Heavy usage: $299/mo (Places API ~$100 + Hunter.io Pro)
